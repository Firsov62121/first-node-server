extends layout.pug

block styles
  link(rel='stylesheet' href='/css/all.css')

block scripts
  script(src='/js/shareCode.js' type='module' defer)
  script(src='/js/hall.js' type='module' defer)

block content
  .ui.segment.new-film-form
    h1 Новый зал:
    form.ui.form#new-hall-form(action="/hall" method="POST")
      .field
        label Число мест в рядах через пробел:
        textarea#hall-cols(rows="2") 
      button.ui.tiny.teal.button Добавить

  .ui.divider.divider_big

  div
    h3.ui.header Текущие залы:
    div#halls Пока нет залов.
